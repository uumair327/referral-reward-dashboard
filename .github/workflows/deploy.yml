name: ⚡ Fast Deploy to GitHub Pages

on:
  push:
    branches: [ main, master ]

env:
  NODE_VERSION: '20'
  FORCE_COLOR: 1

jobs:
  # 🏗️ Build and Deploy Job
  deploy:
    name: 🏗️ Build & Deploy
    runs-on: ubuntu-latest
    
    # Set permissions for GitHub Pages deployment
    permissions:
      contents: read
      pages: write
      id-token: write
    
    # Configure environment for GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: 📦 Install dependencies (Fast)
      run: npm ci --ignore-engines --prefer-offline --no-audit --no-fund
      
    - name: 🏗️ Build for production (Optimized)
      run: |
        echo "🔨 Building with cache busting..."
        export BUILD_TIMESTAMP=$(date +%s)
        npm run build
        echo "✅ Build completed at $(date)"
      
    - name: 🔧 Prepare for deployment (Cache Busting)
      run: |
        # Copy 404 page
        cp src/404.html dist/referral-reward-dashboard/404.html 2>/dev/null || echo "404.html not found, skipping"
        
        # Disable Jekyll
        echo '' > dist/referral-reward-dashboard/.nojekyll
        
        # Add cache busting headers
        echo "Cache-Control: no-cache, no-store, must-revalidate" > dist/referral-reward-dashboard/_headers
        echo "Pragma: no-cache" >> dist/referral-reward-dashboard/_headers
        echo "Expires: 0" >> dist/referral-reward-dashboard/_headers
        
        # Add deployment timestamp
        echo "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > dist/referral-reward-dashboard/deployment-time.txt
        
        echo "🚀 Deployment prepared with timestamp: $(date)"
        
    - name: 🔧 Setup Pages
      uses: actions/configure-pages@v4
      
    - name: 📤 Upload to GitHub Pages (Fast)
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./dist/referral-reward-dashboard/
        
    - name: 🚀 Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      
    - name: ✅ Deployment Success
      run: |
        echo "🎉 Deployment successful!"
        echo "🌐 Your app is live at: ${{ steps.deployment.outputs.page_url }}"
        echo "🔑 Admin access: ${{ steps.deployment.outputs.page_url }}admin (password: admin123)"